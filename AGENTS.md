# AGENTS.md

## 目的

このリポジトリは、占星術の既存 Python ロジックを Web から呼べるテスト運用版として扱う。

## ディレクトリ

- `frontend/`: Stitch ベースの UI。Vite で起動する。
- `backend/`: FastAPI API。本番ではここを単独配備できる前提で編集する。
- `scripts/`: 既存の鑑定ロジックと CSV ローダー。破壊的な変更は避ける。
- `database/`: マスタ CSV / prompt。
- `output/`: ローカル出力用。API では一時ディレクトリを使う。

## 実行方法

- Backend: `python3 -m uvicorn backend.app.main:app --reload --host 0.0.0.0 --port 8000`
- Frontend: `cd frontend && npm run dev -- --host 0.0.0.0 --port 5173`
- Tests: `python3 -m unittest backend.tests.test_api -v`

## ルール

- フロントとバックは分離を維持する。
- API キーや秘密情報をコードへ直書きしない。`.env` を使う。
- `pyswisseph` が入らない環境では live reading は動かない。検証時は API の起動確認とユニットテストを優先する。
- Stitch の見た目を大きく壊さず、色・余白・書体の方針を維持する。
- 既存の `scripts/` は再利用を優先し、大規模な全面改修は避ける。
- 本番移行を見据え、API URL と CORS は環境変数で管理する。

## 作業環境と Git 操作

- 起動中の実作業フォルダでは `npm ci`、`npm install`、`node_modules` の削除を行わない。
- フロントエンドの依存関係インストール、テスト、ビルド検証は、原則として専用の一時 worktree で行う。
- 実作業フォルダの依存関係を修復する必要がある場合は、先にフロントエンドの起動プロセスと待受ポートを確認し、フロントエンドを停止してから実行する。完了後は再起動し、待受ポートと通常版・V2の実ページ表示まで確認する。
- コミットおよび push は、ユーザーから明示された場合にのみ行う。変更依頼だけをコミットや push の許可として扱わない。
- コミット前に `git status` と対象差分を確認し、ユーザーが指示していないファイルや別タスクの変更をステージ・コミット・pushしない。
- 依存関係や起動環境を操作する前に、対象ディレクトリ、関連プロセス、待受ポートを確認する。

## 進捗管理

- フロントエンドの実ページ、または `C:\Users\morii\OneDrive\デスクトップ\obsidian\test1\進捗一覧.md` に登録済みの項目に直接関係するタスクの場合のみ、進捗一覧を確認・更新する。
- バックエンド、調査、雑談など、登録済み項目と直接関係しないタスクでは進捗一覧を確認・変更しない。
- 今回の作業に該当する項目の状態を `進行中` にし、`Codexタスク` に現在のタスク名を記録する。
- 作業完了時に該当項目の状態を `完了` にし、`対応内容` に実施内容を簡潔に記録する。
- 確認や判断が必要な場合は、状態を `確認待ち` にして必要な内容を `対応内容` に記録する。
- 対象項目が一覧にない場合は、自動で追加しない。
- 今回の作業と関係のない項目や、別タスクが更新した内容は変更しない。
- 進捗管理用のMarkdownを新しく増やさず、上記の `進捗一覧.md` に集約する。
