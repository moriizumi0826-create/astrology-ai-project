# `/api/readings` 速度改善・修正進捗

## 目的

`ホロスコープを計算する` の実行から次ページへ遷移するまでの待ち時間を短縮する。

まず `/api/readings` 内の重複計算を削減し、表示内容・スコア・文章・抽出条件を変えずに処理時間を改善する。その後、必要に応じて `/api/yearly-forecast` の遅延取得とキャッシュを検討する。

## 改善時の原則

- 占星術ロジック、スコア、表示件数、文章選択結果を変えない。
- キャッシュキーには天体、日時、タイムゾーン、出生情報など、結果を決定する条件をすべて含める。
- CSVの再読み込み時には、関連キャッシュを必ず無効化・再構築する。
- 一度に複数の最適化を入れず、段階ごとに計測・テスト・差分確認を行う。
- `scripts/` の既存鑑定ロジックは大規模改修せず、backend側の共有・キャッシュを優先する。

## 基準値

計測日: 2026-07-19

計測条件:

- 生年月日: 1984-08-26
- 出生時刻: 19:20
- 出生地: Tokyo
- 緯度経度: 35.6812, 139.7671
- タイムゾーン: UTC+9

| 計測対象 | 処理時間 |
|---|---:|
| `/api/readings` 初回 | 約11.2〜12.1秒 |
| `/api/readings` 同条件2回目 | 約9.4秒 |
| `/api/yearly-forecast` 単独 | 約10.4秒 |
| 両APIの同時実行 | 約22.3秒 |

本番環境では、コールドスタート・CPU性能・通信時間が加わり、画面遷移まで約30秒かかる場合がある。

## 確認済みの重複処理

| 対象 | 総実行数 | ユニーク数 | 重複数・状況 |
|---|---:|---:|---:|
| 天体位置計算 | 24,156 | 6,961 | 17,195回、約71.2%が重複 |
| アスペクト解釈呼び出し | 1,404 | 179 | 1,225回、約87.3%が同一条件 |
| `build_chart_rows` | 12 | 1出生情報 | 同じ出生図を繰り返し構築 |
| `_build_natal_aspect_points` | 9 | 1出生情報 | 同じネイタル点を繰り返し構築 |
| `build_transit_aspect_inputs` | 7 | 当日＋週間 | 当日と週間表示で個別生成 |
| `_build_slot_interpretations` | 25 | 25時間枠 | 72時間を3時間刻みで計算 |
| カウントダウン判定 | 187 | 185 | 候補ごとに軌道を個別走査 |

### DataFrame全件変換

| データ | 行数×列数 | 1リクエスト内の変換回数 |
|---|---:|---:|
| アスペクトマスター | 62,157×22 | 3回 |
| トランジットカレンダー | 3,285×13 | 8回 |
| 逆行イベントカレンダー | 91×14 | 10回 |
| デイリールール | 38×11 | 3回 |

## 比較計測

各機能を実行時だけ一時的に差し替えて計測した結果。処理範囲が重なるため、短縮時間は単純合算しない。

| 比較条件 | 処理時間 | 基準との差 |
|---|---:|---:|
| 通常 | 約12.1秒 | — |
| 天体位置をリクエスト内でメモ化 | 約8.7秒 | 約3.4秒短縮 |
| マスター検索を事前キャッシュ | 約8.9秒 | 約3.2秒短縮 |
| カウントダウン走査を除外 | 約7.6秒 | 約4.5秒分を占有 |
| デイリーパフォーマンスを除外 | 約10.0秒 | 約2.1秒分を占有 |
| 週間アスペクトを除外 | 約10.8秒 | 約1.3秒分を占有 |
| 天体イベントカレンダーを除外 | 約10.5秒 | 約1.6秒分を占有 |

## 修正ロードマップ

| 優先度 | フェーズ | 状態 | 目標 |
|---:|---|---|---|
| 1 | 天体位置のリクエスト内キャッシュ | 完了 | 同一条件の `swisseph.calc_ut` 再実行をなくす |
| 2 | マスター検索インデックスの共通化 | 完了 | 62,157行の全件変換をリクエストごとに行わない |
| 3 | 出生図・ネイタル点の使い回し | 完了 | 同じ出生図の12回構築を1回へ近づける |
| 4 | カウントダウン軌道走査の共有 | 完了 | 候補ごとの重複天体計算を削減する |
| 5 | カレンダー系マスターのインデックス化 | 完了 | 3,285行・91行の繰り返し変換をなくす |
| 6 | 初回遷移に不要な計算の遅延取得 | 完了 | 初回画面遷移を鑑定本体の完了後に行う |
| 7 | `/api/yearly-forecast` のキャッシュ・最適化 | 完了 | 365日分の再計算頻度を減らす |

## フェーズ1: 天体位置のリクエスト内キャッシュ

状態: 完了

対象:

- `backend/app/services/reading_service.py`
- `_calc_transit_planet_motion`
- `_calc_transit_planet_state`

実施案:

- 1リクエスト中だけ有効なキャッシュを用意する。
- キーを `(planet, sample_local_dt, timezone_offset)` とする。
- longitudeとspeedを一度だけ計算し、カウントダウン・デイリーパフォーマンス・週間表示・天体イベントで共有する。
- プロセス全体の無期限キャッシュにはせず、メモリ増加や時刻条件の混同を防ぐ。

確認項目:

- [x] キャッシュ有無で返却JSONが一致する。
- [x] 逆行判定が一致する。
- [x] 日付・タイムゾーンが異なる計算を別キーとして扱う。
- [x] 同一条件の天体位置計算をリクエスト内で再利用する。
- [x] APIテスト70件がすべて成功する。

目標:

- `/api/readings` を約3秒短縮する。

実施結果:

- `ContextVar` を使用し、キャッシュを1リクエスト内に限定した。
- キーは `(正規化済み天体, ローカル日時, タイムゾーンオフセット)` とした。
- 同一リクエスト内でネストした処理は同じキャッシュを共有する。
- リクエスト終了時にキャッシュを破棄し、次のリクエストへ持ち越さない。
- 固定時刻でキャッシュ有無の返却JSONが一致することを確認した。
- 制御計測のウォーム中央値は約3.898秒から約2.766秒へ短縮した。
- 短縮幅は約1.132秒、約29.0%。
- キャッシュ内のユニーク天体位置は6,602件だった。
- 別プロセスの初回実測は約3.528秒、同一プロセスの2回目以降は約2.542秒だった。

## フェーズ2: マスター検索インデックスの共通化

状態: 完了

対象:

- `_ASPECT_CANDIDATES_BY_KEY`
- `_build_master_timeline_advise_lookup`
- `_build_master_pressure_score_lookup`
- `reload_master_dataframes_if_changed`

実施案:

- アスペクトマスター読込時に、records変換を1回だけ行う。
- 同じrecordsから以下を同時構築する。
  - アスペクト候補インデックス
  - `timeline_advise` 検索辞書
  - `Pressure_Score` 検索辞書
- CSV更新時に3つのインデックスを同時に再構築する。

確認項目:

- [x] CSV更新時に3索引とアスペクト解釈キャッシュを無効化する。
- [x] 欠損値・重複キーの既存選択規則を維持する。
- [x] 62,157行の全件辞書化がウォームリクエストでは0回になる。
- [x] 並行する初回構築をLockで直列化する。
- [x] APIテスト72件がすべて成功する。

目標:

- コールド・ウォーム双方で約2〜3秒短縮する。

実施結果:

- アスペクトマスターのrecords変換を初回構築時の1回に統合した。
- 同じrecordsからアスペクト候補、`timeline_advise`、`Pressure_Score` の3索引を構築する。
- 通常のウォームリクエストでは62,157行の全件変換が2回から0回になった。
- CSV再読み込み時に3索引とアスペクト解釈キャッシュを無効化する。
- 初回構築はLockで保護し、並行リクエストによる二重構築を防止する。
- 固定時刻で旧方式と新方式の返却JSONが一致することを確認した。
- 制御計測のウォーム中央値は約2.607秒から約1.656秒へ短縮した。
- 短縮幅は約0.951秒、約36.5%。

## フェーズ3: 出生図・ネイタル点の使い回し

状態: 完了

対象:

- `generate_readings`
- `build_chart_rows`
- `_build_natal_aspect_points`
- `_build_natal_planet_rows`
- `_build_celestial_event_calendar`
- `build_transit_aspect_inputs`

実施案:

- `generate_readings` 冒頭で出生図を1回構築する。
- chart rows、ネイタル天体、ハウスカスプをリクエストコンテキストとして後続処理へ渡す。
- 引数を省略した既存呼び出しは現状動作を維持し、段階的に共有へ切り替える。

確認項目:

- [x] `build_chart_rows` の呼び出し回数を12回から1回へ削減する。
- [x] 出生時刻不明の場合の既存ハウス・ASC・MCテストが成功する。
- [x] ネイタル天体のlongitudeとhouseを既存処理から変更しない。
- [x] 出生条件・タイムゾーンが異なる場合は別キャッシュキーとして扱う。
- [x] APIテスト73件がすべて成功する。

目標:

- 同一リクエスト内の出生図構築を原則1回にする。

実施結果:

- 出生図、ネイタル天体行、ASC・MCを含むアスペクト点を1リクエスト内で共有するコンテキストを追加した。
- キャッシュキーには生年月日、出生時刻、出生時刻不明フラグ、出生地、緯度、経度、タイムゾーンを含めた。
- リクエスト終了時にキャッシュを破棄し、次のリクエストへ持ち越さない。
- `build_chart_rows` の実行回数は12回から1回になった。
- 固定時刻で旧方式と新方式の返却JSONが一致することを確認した。
- 制御計測の中央値は約4.540秒から約4.481秒へ短縮した。
- 短縮幅は約0.059秒、約1.3%。出生図構築の重複は解消したが、主要な時間要因ではないことを確認した。

## フェーズ4: カウントダウン軌道走査の共有

状態: 完了

対象:

- `build_countdown_data`
- `_scan_lunar_countdown_arrival`
- `_scan_lunar_countdown_departure`
- 長期天体のarrival・departure走査
- pressure・relief・negative候補選択

実施案:

- Priority、Score_Impact、対象天体など、天体計算を必要としない条件で先に候補を絞る。
- 同一天体・同一時刻の軌道サンプルを候補間で共有する。
- arrival、departure、pressure、reliefで同じ軌道配列を再利用する。
- 表示対象外になる候補を、可能な範囲で走査前に除外する。

注意点:

- `limit=999` を単純に縮小すると、後段のpressure・relief候補が欠落する可能性がある。
- 表示上位だけでなく、離脱中・影響中判定に必要な候補を保持する。

確認項目:

- [x] countdown、relief、pressureの各IDと並び順が一致する。
- [x] 到来・離脱日時が一致する。
- [x] 月の2時間刻み判定が維持される。
- [x] 年境界をまたぐ走査結果が一致する。
- [x] APIテストがすべて成功する。

目標:

- カウントダウン関連の約4.5秒を半分以下へ近づける。

実施内容・結果:

- 同一天体・日時・タイムゾーン・ネイタル経度・アスペクト角のオーブ計算を、1リクエスト内で共有するキャッシュを追加した。
- 実測で184回呼ばれていたカウントダウンマスターのDataFrame絞り込みを、CSV読み込み単位で1回構築するTrigger_ID索引へ変更した。
- マスターCSV再読み込み時は索引を破棄し、次回参照時に再構築する。
- 固定時刻の返却JSONは変更前後で同一（SHA-256: `adcba817bdc96f1c2385464336737bdc28632ca81aa969a25d0e3d48fba43759`）。
- APIテスト75件がすべて成功した。
- 全体中央値は約4.280秒から約3.399秒へ短縮（約20.6%）。
- カウントダウン相当時間は約1.965秒から約1.042秒へ短縮（約47.0%）。

## フェーズ5: カレンダー系マスターのインデックス化

状態: 完了

実施案:

- トランジットカレンダーを日付・天体・イベント種別で索引化する。
- 逆行イベントカレンダーを天体・イベント種別で索引化する。
- `DataFrame.to_dict("records")` を各天体ループ内で呼ばない。
- CSV再読み込み時に索引を再構築する。

確認項目:

- [x] 逆行開始・順行復帰日が一致する。
- [x] Next Stellar Eventの並び順と件数が一致する。
- [x] 日付境界と対象期間外除外が一致する。

実施内容・結果:

- トランジットカレンダー3,285行から、逆行開始日を天体別に参照できる索引をCSV読み込み単位で構築するよう変更した。
- 逆行イベントカレンダー91行を一度だけ日付正規化・並べ替えし、天体・イベント種別の組み合わせで参照できる索引を追加した。
- 逆行イベントの全件表示、天体指定、逆行開始／順行開始指定で同じ索引を共有する。
- マスターCSV再読み込み時に両索引を破棄し、次回参照時に再構築する。
- 固定時刻の返却JSONはフェーズ4完了時と同一（SHA-256: `adcba817bdc96f1c2385464336737bdc28632ca81aa969a25d0e3d48fba43759`）。
- APIテスト76件がすべて成功した。
- ウォーム実行の全体中央値は約3.399秒から約2.468秒へ短縮（フェーズ5単体で約27.4%）。

## フェーズ6: 初回遷移に不要な計算の遅延取得

状態: 完了

候補:

- デイリーパフォーマンス
- 週間アスペクト
- Next Stellar Event
- 年間予測

実施案:

- `/api/readings` の基本結果が完成した時点で次ページへ遷移する。
- 重いウィジェットは遷移後に取得し、対象領域にローディング表示を出す。
- 取得失敗時は既存データを保持し、対象ウィジェット単位でエラーを表示する。

注意点:

- API分割前にフェーズ1〜5を行い、分割が本当に必要か再計測する。
- データ保存形式と最新版更新処理への影響を確認する。

実施内容・結果:

- `/api/readings?defer_widgets=true` を追加し、初回レスポンスではデイリーパフォーマンス、週間アスペクト、天体イベントカレンダーを空配列として返すコア鑑定モードを実装した。
- 遅延対象だけを返す `/api/readings/deferred` を追加した。
- 入力画面では年間予測との `Promise.all` を廃止し、コア鑑定の保存完了後に直ちに詳細ページへ遷移するよう変更した。
- 詳細ページで年間予測と遅延ウィジェットを並列取得し、成功したデータだけを既存保存結果へ統合するよう変更した。
- 遅延取得中はローディング表示、失敗時は既存データを保持したまま取得対象を明示したエラーを表示する。
- フルモードの固定時刻JSONはフェーズ5完了時と同一（SHA-256: `adcba817bdc96f1c2385464336737bdc28632ca81aa969a25d0e3d48fba43759`）。
- `/api/readings` のウォーム中央値はフル約2.506秒、初回用コア約1.299秒（約48.2%短縮）。遅延ウィジェットは約1.315秒で遷移後に実行される。
- 実ブラウザでは入力送信から詳細ページ表示まで約2.424秒で遷移し、その後のウィジェット反映とエラーなしを確認した。
- APIテスト79件、年間予測テスト19件がすべて成功し、フロントエンド本番ビルドも成功した。

## フェーズ7: `/api/yearly-forecast` の改善

状態: 完了

実施案:

- 出生情報＋対象年＋マスターバージョンをキーに年間予測をキャッシュする。
- 月間ピークルールを毎日全件照合せず、条件別インデックスを構築する。
- 同じ出生図・ネイタル点を年間365日で共有する。
- 初回入力時は遷移後に取得する。

実施内容・結果:

- 同一の出生情報・対象年・鑑定マスター署名・年間予測マスター署名をキーに、直近1件の年間予測をプロセス内で再利用するキャッシュを実装した。年間JSONが大きいため、保持件数は1件に制限した。
- 鑑定マスター、年間予測マスター、月間ピーク関連CSVの変更時に年間予測キャッシュを無効化するようにした。
- 月間ピーク集計で毎日繰り返していたスコアリングルール検索とカテゴリ校正を、CSV更新単位の事前計算へ変更した。
- 既存の候補ルール索引で確認済みの9条件を再照合せず、日次処理では残るorb条件だけを確認するようにした。
- 出生図・ネイタル点は従来どおり年間処理の冒頭で1回だけ構築し、365日で共有していることを確認した。
- 初回入力からの遷移はフェーズ6で年間予測の完了を待たない構成へ変更済み。
- 固定入力JSONは最適化前後で完全一致（SHA-256: `9192d32216ebd1711ffa153c5c3c556d9d8d841683ef1d0e2e2f46f8ad51f7ec`、28,445,457 bytes）。
- 未キャッシュのウォーム中央値は約3.608秒から約2.483秒へ約31.2%短縮。キャッシュヒット時は約0.010秒。
- コールド実測は約11.030秒から約9.523秒へ約13.7%短縮した。
- APIテスト79件、年間予測テスト21件、Pythonコンパイル確認、フロントエンド本番ビルドがすべて成功した。

## 検証方法

### 機能テスト

```powershell
python -m unittest backend.tests.test_api -v
```

必要に応じて年間予測テストも実行する。

```powershell
python -m unittest backend.tests.test_yearly_forecast -v
```

### 性能計測

各フェーズで最低3回計測し、中央値を記録する。

記録する値:

- `/api/readings` コールド時
- `/api/readings` ウォーム時
- `/api/yearly-forecast`
- 2API同時実行
- 天体位置計算の総数とユニーク数
- `build_chart_rows` 呼び出し回数
- 大規模DataFrameのrecords変換回数

### 結果同一性

最適化前後で以下を比較する。

- `chart_data`
- `readings`
- `dashboard_data.dailyPerformance`
- countdown関連全項目
- pressure・relief関連全項目
- weekly aspects
- celestial event calendar
- planet motion・retrograde calendar

動的な現在時刻・master version・生成時刻を除き、JSON内容が一致することを確認する。

## 完了条件

- [x] `/api/readings` のウォーム時中央値が6秒以下、または現状から40%以上短縮する。
- [x] `/api/readings` のコールド時中央値が8秒以下、または現状から30%以上短縮する。
- [x] 既存APIテストがすべて成功する。
- [x] 最適化前後で占星術上の出力が一致する。
- [x] CSV更新後にキャッシュが正しく更新される。
- [ ] 本番環境で `ホロスコープを計算する` から遷移までを再計測する。
- [x] 修正内容と最終計測値をこのファイルへ記録する。

## 作業履歴

| 日付 | 状態 | 内容 | 計測結果 |
|---|---|---|---|
| 2026-07-19 | 調査完了 | 重複回数、主要機能の処理時間、コールド・ウォーム差を計測 | 初回約11.2〜12.1秒、2回目約9.4秒 |
| 2026-07-19 | 計画作成 | 修正フェーズ、検証方法、完了条件を整理 | 実装は未着手 |
| 2026-07-19 | フェーズ1完了 | 天体位置・速度をリクエスト内で共有し、終了時に破棄するキャッシュを実装 | ウォーム中央値約3.898秒→約2.766秒、約29.0%短縮、APIテスト70件成功 |
| 2026-07-19 | フェーズ2完了 | アスペクトマスターのrecords変換と3検索索引を共通化し、CSV再読み込み時の無効化を実装 | ウォーム中央値約2.607秒→約1.656秒、約36.5%短縮、APIテスト72件成功 |
| 2026-07-19 | フェーズ3完了 | 出生図・ネイタル天体・アスペクト点をリクエスト内で共有 | `build_chart_rows` 12回→1回、中央値約4.540秒→約4.481秒、APIテスト73件成功 |
| 2026-07-19 | フェーズ7完了 | 年間予測の直近結果キャッシュ、月間ピーク集計コンテキストの事前計算、マスター更新時の無効化を実装 | 未キャッシュ中央値約3.608秒→約2.483秒、キャッシュ時約0.010秒、API79件・年間予測21件成功 |

## 判断・変更記録

| 日付 | 対象 | 判断内容 | 理由 |
|---|---|---|---|
| 2026-07-19 | 最適化順序 | キャッシュ・インデックス・出生図共有を先に実施する | 表示仕様を変えずに改善できるため |
| 2026-07-19 | API分割 | 初期フェーズでは実施しない | まず内部の明確な重複を削減し、再計測してから判断するため |
