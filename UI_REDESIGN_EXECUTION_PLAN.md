# UIリデザイン実行計画書

> 2026-05-19 方針転換: この計画は既存UIの部分改善用として停止する。以後は `DASHBOARD_REBUILD_PLAN.md` のゼロベース統合ダッシュボード再設計計画を優先する。

この計画書は、フロントエンドUI改修中に作業範囲が崩れたり、意図しない箇所へ変更が広がったりすることを防ぐための進行管理メモです。  
実装前に必ずこの計画を確認し、対象範囲を固定してから作業する。

## 基本方針

一度に全体を変えない。  
まず1つの対象だけを完成形に近づけ、その見た目とルールを確認してから次へ進む。

優先する順番。

1. PC版 2026運勢シミュレーション
2. スマホ展開版 2026運勢シミュレーション
3. スマホ縮小パネル 運勢スコア
4. Personal Reading
5. カウントダウン
6. ロジック安定指標
7. リソース最適化・タイムライン
8. トピック強化カード

## 参照するルール

作業時は以下を参照する。

- `デザインルール.md`
- 既存のUI実装
- 直近でユーザーが正とした表示

判断に迷った場合、勝手に補完せず確認する。

## 絶対に守ること

### 変更範囲を固定する

作業前に必ず対象を一文で明示する。

例。

```txt
今回はPC版の2026運勢シミュレーションだけを変更する
スマホ版、縮小パネル、Personal Readingには触らない
```

### 似たUIを勝手に同期しない

以下は別対象として扱う。

- PC版
- スマホ展開版
- スマホ縮小パネル
- 旧表記
- 新表記
- Personal Reading展開後
- Personal Reading縮小パネル

ユーザーが `同様に` `合わせて` `共通で` と明示した時だけ、複数対象に変更を広げる。

### 勝手な改善をしない

以下は禁止。

- 指示されていない文言変更
- 指示されていない色変更
- 指示されていないレイアウト変更
- ついでのリファクタ
- 似たコンポーネントへの横展開
- ロジック変更
- データ構造変更

## 作業単位

1回の作業では、原則として1テーマだけ扱う。

良い例。

```txt
PC版 2026運勢シミュレーションの詳細本文エリアだけ調整
```

悪い例。

```txt
PC版を直すついでにスマホ縮小パネルも統一
```

## フェーズ1 PC版 2026運勢シミュレーション

### 対象

PC版の展開表示にある `2026運勢シミュレーション`。

### 対象外

- スマホ展開版
- スマホ縮小パネル
- 旧表記
- Personal Reading
- カウントダウン
- ロジック安定指標
- バックエンド
- CSV

### 目的

PC版で以下を見やすく整理する。

- 日付
- 総合スコア
- 2x2ジャンルスコア
- 折れ線グラフ
- 短期テーマ / 長期テーマ切り替え
- ジャンル切り替え
- 本文
- Advised Task

### 作業候補

- スコア枠の余白調整
- グラフ枠の余白調整
- 切り替えUIと本文の重なり防止
- 本文の読みやすい行幅調整
- Advised Taskのカード感調整

### 完了条件

- PC幅で本文と切り替えUIが重ならない
- 総合スコアが右上に分かりやすく表示される
- 2x2ジャンルスコアが崩れない
- グラフの総合線が他ジャンルと区別できる
- `npm run build` が通る

### タスクリスト

| No | 状態 | タスク | 対象 | 完了条件 |
| --- | --- | --- | --- | --- |
| 1 | 完了 | 改修前ログ作成 | `layout-change-logs/pc-yearly-forecast-before_20260519_180255.md` | 対象範囲、対象外、現状構成、ロールバック基準が記録されている |
| 2 | 完了 | PC版スコア枠の視覚整理 | `frontend/src/yearly-forecast.jsx` のPC版スコア枠 | 日付、総合スコア、2x2ジャンルスコアが見やすくまとまる |
| 3 | 次に着手 | PC版グラフ枠の余白整理 | `frontend/src/yearly-forecast.jsx` のグラフ枠 | グラフ上下左右の余白が過剰でなく、凡例と干渉しない |
| 4 | 未着手 | PC版切り替えUIの配置整理 | 詳細本文枠の短期/長期、ジャンル切り替え | 本文と重ならず、操作対象として分かりやすい |
| 5 | 未着手 | PC版本文とAdvised Taskの階層整理 | 詳細本文枠 | タイトル、本文、Advised Taskの視線誘導が自然 |
| 6 | 未着手 | PC版のみビルド確認 | `frontend` | `npm run build` が通る |
| 7 | 未着手 | 進行ログ追記 | `UI_REDESIGN_EXECUTION_PLAN.md` | 実施内容、触ったファイル、検証結果が残っている |

## フェーズ2 スマホ展開版 2026運勢シミュレーション

### 対象

スマホで縮小パネルを開いた後の `2026運勢シミュレーション`。

### 対象外

- スマホ縮小パネル
- PC版
- 旧表記
- 他パネル

### 目的

スマホ展開表示で、PC版と同じ情報を縦方向に読みやすく整理する。

### 作業候補

- 日付と総合スコアの配置確認
- 2x2ジャンルスコアの折返し確認
- グラフ高さと余白調整
- 切り替えUIの横幅調整
- 本文とAdvised Taskの余白調整

### 完了条件

- 横スクロールが出ない
- ラベルが不自然に折れない
- スコアが読みやすい
- グラフ全体が見える
- `npm run build` が通る

## フェーズ3 スマホ縮小パネル 運勢スコア

### 対象

スマホ新表記の縮小パネル `運勢スコア`。

### 対象外

- 展開後
- PC版
- 他の縮小パネル

### 目的

1秒で状態が分かる縮小パネルにする。

### 表示内容

- 見出し `運勢スコア`
- 右上に総合スコア
- 2x2ジャンルスコア
- 小グラフ

### 完了条件

- 総合スコアが右上に見える
- 2x2ジャンルが指定順で表示される
- 小グラフに5本の線がある
- 文字が潰れない
- `npm run build` が通る

## フェーズ4 Personal Reading

### 対象

Personal Readingの縮小パネルと展開後。

### 注意

縮小パネルと展開後は別対象。  
両方変更する場合は、ユーザー指示または計画内で明示する。

### 現行の文言基準

- 見出し `今日はどんな日？`
- タブまたはラベル `本日の星模様`
- タブまたはラベル `本日の重要ポイント`

### 完了条件

- 見出し文言が揃っている
- タブ名が揃っている
- 旧表記を壊していない
- `npm run build` が通る

## 検証手順

各作業後に最低限行う。

1. `git diff` で対象外ファイルが混ざっていないか確認
2. 対象コンポーネントの差分を確認
3. `npm run build`
4. 必要ならブラウザ確認

ブラウザ確認を行う場合は、確認対象を明示する。

例。

```txt
PC幅で2026運勢シミュレーションのみ確認
```

## ロールバック方針

問題が出た場合は、対象作業で触った差分だけ戻す。  
ユーザーや過去作業の変更をまとめて戻さない。

戻す前に確認すること。

- どのファイルを触ったか
- どのブロックを触ったか
- 対象外の変更が混ざっていないか

## 作業開始テンプレート

作業前に以下を宣言する。

```txt
今回の対象:
今回触らないもの:
完了条件:
```

例。

```txt
今回の対象: PC版 2026運勢シミュレーションの詳細本文エリア
今回触らないもの: スマホ版、縮小パネル、Personal Reading、バックエンド
完了条件: 切り替えUIが本文に重ならず、ビルドが通る
```

## 進行ログ

作業したら以下の形式で追記する。

```txt
日付:
対象:
変更内容:
触ったファイル:
検証:
残課題:
```

このログにより、途中で状態が分からなくなることを防ぐ。

---

日付: 2026-05-19 18:02:55  
対象: PC版 2026運勢シミュレーション改修前ログ作成  
変更内容: 改修前の対象範囲、現状構成、対象外、懸念点、次作業候補、ロールバック基準を記録  
触ったファイル: `layout-change-logs/pc-yearly-forecast-before_20260519_180255.md`, `UI_REDESIGN_EXECUTION_PLAN.md`  
検証: 見出し構成と対象範囲を確認予定  
残課題: 次はPC版スコア枠の視覚整理から着手するか、ブラウザで現状スクリーンショットを取得する

---

日付: 2026-05-19  
対象: PC版 2026運勢シミュレーションのスコア枠  
変更内容: 日付と総合スコアを同一ヘッダー行に整理し、2x2ジャンルカードのアイコン、余白、高さ、数値サイズを揃えた  
触ったファイル: `frontend/src/yearly-forecast.jsx`, `UI_REDESIGN_EXECUTION_PLAN.md`  
検証: `npm run build` 成功。差分はPC版スコア枠ブロックのみ確認  
残課題: 次はPC版グラフ枠の余白整理
